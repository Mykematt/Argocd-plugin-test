steps:
  - label: "🔄 Step 5: Explicit Manual Rollback"
    key: "explicit-manual-rollback"
    agents:
      queue: kubernetes
    plugins:
      - secrets#v1.0.0:
          variables:
            ARGOCD_PASSWORD: argocd_password
      - github.com/Mykematt/argocd-deployment-buildkite-plugin#7acb6e8:
          app: "test-nginx-app"
          argocd_server: "argocd-server.argocd.svc.cluster.local:443"
          argocd_username: "admin"
          mode: "rollback"
          rollback_mode: "manual"
          target_revision: "3c8a622bfdc1dcbe7274f54461447e33ae37a9ae"
          timeout: 300
          collect_logs: true
          upload_artifacts: true